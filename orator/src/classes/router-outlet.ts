import { customElement } from '@ornament/decorators';
import { OratorCustomElementTag } from 'src/enums';

const html = ``;
const styles = ``;

@customElement({
  tag: OratorCustomElementTag.routerOutlet,
  html,
  styles
})
export class RouterOutlet extends HTMLElement {

}
